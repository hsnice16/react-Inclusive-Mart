/**
 * isStatusLoading - function to check the
 *                   status is loading or not
 *
 * @param {string} status - status to check
 * @returns a bool true/false
 */
const isStatusLoading = (status) => status === "loading";

export { isStatusLoading };
