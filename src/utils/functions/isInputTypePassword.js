/**
 * isInputTypePassword - function to check the
 *                      type is password or not
 *
 * @param {string} type - input type to check
 * @returns a bool true/false
 */
const isInputTypePassword = (type) => type === "password";

export { isInputTypePassword };
